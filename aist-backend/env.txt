# Шаблон переменных окружения (скопируйте в .env и заполните)
# Реальные значения в .env не коммитить в git.
# Один процесс server.js обслуживает и HTTP API, и WebSocket для звонков (путь /ws). Отдельный calls-ws.js не нужен.

PORT=3001
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=aist_app
DB_PASS=ваш_надёжный_пароль
DB_NAME=aist_messenger

JWT_SECRET=длинная_случайная_строка_64_символа
REDIS_URL=redis://127.0.0.1:6379

# Telegram-бот для отправки кода входа (обязательно для продакшена)
TGBOT=токен_бота_от_BotFather
# Секрет для привязки номера к Telegram: бот вызывает POST /api/internal/link-telegram с заголовком X-Link-Secret
TELEGRAM_LINK_SECRET=случайная_строка_для_бота

SMSAERO_EMAIL=email_аккаунта_SMSAERO
SMSAERO_API_KEY=ключ_из_кабинета_SMSAERO
